import React from 'react';
import contactData from '../data';

export default function contact() {
  // let displayData = contactData.map(c => {})
  return (
    <div>
      <h1>contact</h1>
    </div>
  )
}
